"Use Strict"; // treat all JS code as newer version al
// (supports old versions)
 //alert (3+3) wea re useing nodejs , not browser

 // prefer not using terminator in same line code
 // javascript avoids terminator 
// we use to write code in formatted ways
// readablity should be high

//mdn documentaion for javascript docs
// prefred use is 
// https://tc39.es/ecma262/

/*
concise list of data types in JavaScript:

1. Primitive Data Types:
   `Number`
   `String`
   `Boolean`
   `Undefined`
   `Null`

2. Special Values:
   `NaN` (Not a Number)
   `Infinity` and `-Infinity`

3. Complex Data Types:
   `Object`
   `Function`
   `Array`
   `Date`
   `RegExp` (Regular Expression)
   `Symbol` (introduced in ECMAScript 6)
*/


let name ="khizar"
let age=24
let isLoggedIn =true

// number => 2 to the power 53
// bigint .. for trading .. nad large websites
// string => ""
//bolean => true/false
// null .. is dataType and  value in javascript 
// undefined is not ( null )
// symbol   => unique 


// object 

console.log(typeof null);  
// returns object .. null is object 

console.log(typeof undefined)
//    returns undefined
 


